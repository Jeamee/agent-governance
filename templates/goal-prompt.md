# Delivery goal template

```text
Outcome: implement design_ref={repository, commit, path, digest} completely.
Constraints: do not modify frozen design, plan baselines, governance paths,
gate configuration, test configuration, coverage/mutation configuration, or
approved screenshot baselines. Declare every execution-route deviation as a
structured DEV record. Stop and return any decision not covered by the design.
Verification: execute every required verifier and provide the delivery request,
CI attestation, evidence, and remaining risks.
```
