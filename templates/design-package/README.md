# Three-layer task design template

Create all files below before implementation. Freeze the whole package in a
human-reviewed PR; the two implementation-plan files remain immutable baselines
and route deviations belong only in a delivery request.
