# Governance schema drafts

These eight JSON Schema documents are M0 drafts of the frozen specifications
in `docs/designs/governance-bootstrap/design-package-v1.1.md` section 5.
They deliberately cover structural validity only. M1 adds the deterministic
cross-file and git-history checks to `governance verify`.
