# M0 adapter skeleton. M2 resolves platform-specific enforcement only here.
# The adapter may checkout a pinned governance revision and invoke the CLI; it
# must not reimplement schema, policy, acceptance, or authorization logic.
adapter: github-actions
entrypoint: governance verify --mode ci
status: reserved-for-m2
